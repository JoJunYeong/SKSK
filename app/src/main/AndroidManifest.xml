<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.co.wero.www.sksk">

    <uses-permission android:name="android.permission.BLUETOOTH" />  <!-- 블루투스때문에 추가한 속성 -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />  <!-- 블루투스때문에 추가한 속성 -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".settings"></activity>
        <activity android:name=".mainpage"
            android:screenOrientation="portrait"/>      <!-- 블루투스때문에 추가한 속성 -->
        <activity android:name=".personal_privacy_policy" />
        <activity android:name=".find_account" />
        <activity android:name=".createidpw" />
        <activity android:name=".login" />
        <activity android:name=".installfirst" />
        <activity android:name=".intro" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>